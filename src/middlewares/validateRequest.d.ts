import type { ZodType } from "zod";
import type { RequestHandler } from "express";
declare const validateRequest: (schema: ZodType) => RequestHandler;
export default validateRequest;
//# sourceMappingURL=validateRequest.d.ts.map