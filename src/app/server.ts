// src/app/server.ts

import { bootstrap } from '../bootstrap/index.js';

/**
 * Application entry point.
 *
 * Responsibilities:
 * 1. Bootstrap the application.
 * 2. Exit immediately if startup fails.
 */
void bootstrap();
