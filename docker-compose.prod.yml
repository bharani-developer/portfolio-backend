version: '3.9'

services:
  api:
    container_name: portfolio-backend

    build:
      context: .
      dockerfile: Dockerfile

    restart: always

    env_file:
      - .env

    ports:
      - '5000:5000'

    networks:
      - portfolio-network

networks:
  portfolio-network:
